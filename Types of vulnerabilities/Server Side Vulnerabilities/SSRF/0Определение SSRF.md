# Карта раздела
[[Карта раздела Types Of Vulnerabilities in Web]]

---
# Оглавление
[[#Последствия SSRF]]
[[#Распространенные атаки SSRF]]
[[#Атаки SSRF на сервер]]
[[#Пример Lab Basic SSRF against the local server]]
[[#Атаки SSRF на другие бэкенд системы]]
[[#Пример Lab Basic SSRF against another back-end system]]
[[#Обход типичных защит от SSRF]]
[[#SSRF с фильтрами ввода на основе черного списка]]
	[[#Пример Lab SSRF with whitelist-based input filter]]
[[#Обход фильтров SSRF через Open Redirect]]
	[[#Пример Lab SSRF with filter bypass via open redirection vulnerability]]
[[#Blind SSRF]]
	[[#Последствия Blind SSRF]]
	[[#Как находить и эксплуатировать Blind SSRF]]
	[[#Пример Lab Blind SSRF with out-of-band detection]]
	

---


> [!note] Определение SSRF
>SSRF(Server Side Request Forgery) - это уязвимость веб безопасности, позволяющая атакующему заставить серверное приложение выполнять запросы к непредусмотренному месту назначения
>В типичной SSRF атакующий может заставить сервер устанавливать соединение с сервисами доступными только внутри структуры организации, в других случаях ему может удаваться принудить сервер подключаться к произвольным внешним системам. Это приводит к утечке конфиденциальных данных, например учетных данных авторизации

# Последствия SSRF
Успешная SSRF часто приводит к несанкционированным действиям или доступу к данным внутри организации, это может происходить как в уязвимом приложении так и на других бекенд-системах, с которыми приложение может взаимодействовать. В некоторых ситуациях уязвимость SSRF может позволить выполнить RCE
# Распространенные атаки SSRF
SSRF часто злоупотребляют доверительными отношениями, чтобы эскалировать атакую от уязвимого приложения и выполнить несанкционированные действия. Эти доверительные отношения могут существовать по отношению к самому серверу или к другим бекэнд системам внутри той же организации
# Атаки SSRF на сервер
В атаке SSRF на сервер атакующий заставляет приложение выполнить HTTP запрос обратно к серверу, на котором размещено приложение, через его loopback. Обычно это делается путем передачи URL с именем хоста вроде 127.0.0.1 или `localhost`

Например представим приложение магазина, которое позволяет пользователю смотреть, имеется ли товар в наличии в конкретном магазине, чтобы предоставить сведения о наличии, приложение должно опрашивать различные REST API бекэнда, оно делает  это передавая URL соответствующего эндпоинта API бекенда через фронтенд HTTP запрос. Когда пользователь просматривает статус наличия товара его браузер выполняет следующий запрос
```
POST /product/stock HTTP/1.0
Content-Type: application/x-www-form-urlencoded
Content-Length: 118
stockApi=http://stock.weliketoshop.net:8080/product/stock/check%3FproductId%3D6%26storeId%3D1
```
Это заставляет сервер выполнить запрос по указанному URL, получить статус наличия и вернуть его пользователю

В этом примере атакующий может изменить запрос указав URL, локальный для сервера:
```
POST /product/stock HTTP/1.0
Content-Type: application/x-www-form-urlencoded
Content-Length: 118
stockApi=http://localhost/admin
```
Сервер извлекает содержимой URL `/admin` и возвращает его пользователю. Атакующий может открыть URL `/admin`, но административная функциональность обычно доступна только аутентифицированным пользователям, значит атакующий ничего интересного не увидит, но если запрос к URL `admin` приходит с локальной машины, тот обычные механизмы контроля доступа обходятся, приложение предоставляет полный доступ к административной функциональности, поскольку запрос выглядит как исходящий из доверенного места

---
## Пример: Lab: Basic SSRF against the local server
В описании сказано что в лабораторной работе имеется функция проверки запасов обращается ко внутреннему `stockApi`, изменяя ее можем получить обращение сервера к самому себе через loopback соответственно можем сами вызвать нужную нам страницу после посмотреть как вызывается удаление пользователя и выполнить лабу

---
Почему приложения ведут себя таким образом и имплицитно доверяют запросам, проходящим с локальной машины? 
Это может происходить по разным причинам:
- Проверка контроля доступа может быть реализована в другом компоненте, который расположен перед сервером приложения. Когда соединение устанавливается обратно к серверу, то такая проверка обходится
- Для целей аварийного восстановления приложение может разрешать административный доступ без входа в систему любому пользователю, приходящему с локальной машины. Это предоставляет администратору способ восстановить систему, если он потеряет свои учетные данные. Предполагается что напрямую с сервера может приходить только полностью доверенный пользователь
- Административный интерфейс может слушать на другом номере порта, отличном от основного приложения, и может быть недоступен напрямую для пользователей

Такие отношения доверия, при которых запросы исходящие с локальной машины обрабатываются иначе, чем обычные запросы, зачастую делают SSRF критической уязвимостью
# Атаки SSRF на другие бэкенд системы
В некоторых случаях сервер приложения способен взаимодействовать с бэкенд системами, которые недоступны напрямую пользователям. Эти системы часто используют нероутируемые приватные IP адреса. Бэкенд системы обычно защищены топологией сети, поэтому нередко имеют более слабую защиту. Во многих случаях внутренние бекэнд системы содержат чувствительную функциональность, доступ к которой можно получить без аутентификации любому кто способен взаимодействовать с этими системами
В предыдущем примере представим, что существует административный интерфейс по бекэнд URL `https://192.168.0.68/admin` атакующий может отправить следующий запрос чтобы эксплуатировать уязвимость SSRF и получить доступ к административному интерфейсу
```
POST /product/stock HTTP/1.0
Content-Type: application/x-www-form-urlencoded
Content-Length: 118
stockApi=http://192.168.0.68/admin
```
## Пример: Lab: Basic SSRF against another back-end system
Нам прямо сказано про уязвимость находится в функции проверке количества товаров, только теперь необходимо найти, панель админа которая скрыта на внутреннем другом IP, поэтому нужно перебрать используем Intruder, находим панель и делаем POST на конечную точку которая удаляет пользователя carlos
# Обход типичных защит от SSRF
Часто встречаются приложения, содержащие поведение SSRF, вместе с защитами направленными на предотвращение эксплуатации уязвимостей. Нередко эти защиты можно обойти
## SSRF с фильтрами ввода на основе черного списка
Некоторые приложения блокируют ввод, содержащий имена хостов вроде `127.0.0.1` и `localhost` или другие чувствительные URL, такие как `/admin`, их часто можно обойти следующими техниками
- Использовать альтернативное представление IP адреса `127.0.0.1`, например `2130706433`, `017700000001` или `127.1`
- Зарегистрируйте собственное доменное имя, которое резолвится в `127.0.0.1`, для этой цели вы можете использовать `spoofed.burpcollaborator.net`
- Обфусцируйте блокируемые строки с помощью URL кодирования или вариаций регистра
- Предоставьте URL, которым вы управляете, и который перенаправляет на целевой URL. Попробуйте разные коды редиректа а так же разные протоколы для целевого URL. Например, переключение с `http` на `https` во время редиректа как показано, позволяет обходить некоторые анти-SSRF фильтры
## Пример Lab: SSRF with blacklist-based input filter
Здесь применяется легкая защита для обхода которой необходимо воспользоваться техниками выше, у меня получилось используя `127.1` и разный регистр для `/admin`
## SSRF с фильтрами на основе белого списка
Некоторые приложения допускают только входные данные соответствующие белому списку разрешенных значений. Фильтр может искать совпадения в начале ввода или наличия подстроки внутри него. Вы можете обойти этот фильтр, эксплуатируя несоответствия в разборе URL

Спецификация URL содержит ряд особенностей, которые вероятно будут упущены из виду при реализации разборов и проверок URL "на коленке" 
- Можно встроить учетные данный в URL перед именем хоста, используя символ `@`. Например: `https://expected-host:fakepassword@evil-host`
- Можно использовать символ `#` для указания фрагмента URL. Например `https://evil-host#expected-host`
- Можно использовать иерархию DNS чтобы разместить требуемый ввод внутри квалифицированного DNS имени которым вы управляете. Например: `https://expected-host.evil-host`
- Вы можете применять URL-кодирование символов, чтобы запутать код разбора URL. Это особенно полезно если код реализующий фильтр обрабатывает URL кодированные символы иначе чем код, выполняющий бекэнд HTTP запрос. Вы так же можете попробовать двойное кодирование символов, некоторые сервера рекурсивно URL декодируют входные данные, что может приводить к дальнейшим расхождениям
- Так же можно комбинировать все эти техники
## Пример Lab: SSRF with whitelist-based input filter
Здесь необходимо поэтапно изменять URL чтобы он подходил под фильтр, тогда получим, первое что сработало это вот такой payload который отдал 500
```
http://localhost%40stock.weliketoshop.net/admin
```
то есть по факту `@` отрезает все что слева от нее поэтому запрос прошел и вернул 500, далее тогда надо как то отрезать то что справа и оставить loacalhost, тогда попробуем `#` но с одиночным кодированием символа у меня не работало поэтому я попробовал два раза и получилось 
```
http://127.0.0.1%25%32%33localhost%40stock.weliketoshop.net/admin
```
localhost тоже сработает, я оставил 127.0.0.1 потому что захотел, после этого `#` отрежет все что справа от него и останется по факту `http://localhost/admin` и после получаем панель админа и удаляем карлоса уже другим запросом 
# Обход фильтров SSRF через Open Redirect
Иногда можно обойти защиты на основе фильтров эксплуатируя уязвимость `Open Redirect`

В предыдущем примере представим, что отправляемый пользователем URL строго валидируется, чтобы предотвратить опасную эксплуатацию поведения SSRF, но в приложении, чьи URL разрешены, присутствует уязвимость Open Redirect. Если API используемый для выполнения бэкенд HTTP запроса поддерживает переадресации, вы можете сформировать URL, который удовлетворяет фильтру и приводит к перенаправленному запросу к желаемой бэкенд-цели
Например в приложении есть уязвимость `Open Redirect` при которой URL
```
/product/nextProduct?currentProductId=6&path=http://evil-user.net
```
Возвращает на 
```
http://evil-user.net
```
В таком случае мы можем использовать уязвимость чтобы обойти URL фильтр и эксплуатировать SSRF следующим образом
```
POST /product/stock HTTP/1.0
Content-Type: application/x-www-form-urlencoded
Content-Length: 118
stockApi=http://weliketoshop.net/product/nextProduct?currentProductId=6&path=http://192.168.0.68/admin
```
Данная SSRF работает потому что приложение сначала проверяет что переданный URL в параметре StockAPI находится на разрешенном домене, что по факту так и есть, затем приложение запрашивает указанный URL, что инициализирует Open Redirect. Приложение следует переадресации и выполняет запрос к внешнему URL, что инициирует Open Redirect, приложение следует переадресация и выполняет запрос к внутреннему URL по выбору атакующего 
## Пример Lab: SSRF with filter bypass via open redirection vulnerability
Нужно применить уязвимость Open Redirect, но сначала нужно найти где в приложении это может быть, по факту, мы имеем что функционал который позволяет перейти на следующий продукт делает ссылку через `path`, но ее можно заменить на нужный нам URL, так он возвращает 302 на адрес `http://192.168.0.12:8080/admin`, но дальше посмотреть не дает, поэтому будем использовать вторую функцию которая принимает `stockAPI` и укажем там наш путь до админ панели и она уже возвращает нужный нам результат, теперь просматриваем код страницы и добавляем в конце нужную часть URL чтобы payload сработал как надо, после чего пользователь карлос удалится
# Blind SSRF 

> [!note] Blind Server-Side Request Forgery
> Уязвимость Blind SSRF возникает когда приложение можно побудить выполнить бекэнд HTTP запрос по переданному URL, но ответ этот бекэнд запрос не возвращает во фронтенд ответе приложения

## Последствия Blind SSRF
Часто уязвимости Blind SSRF нижу чем у полноценных уязвимостей SSRF из за их односторонней природы, их нельзя тривиально использовать для извлечения конфиденциальных данных из бекэнд систем, хотя в некоторых ситуациях их можно эксплуатировать для достижения RCE
## Как находить и эксплуатировать Blind SSRF
Самый надежный способ обнаружения Blind SSRF это применение техник out-of-band, это включает попытку спровоцировать HTTP запрос к внешней системе, которой вы управляете и мониторинг сетевых взаимодействий с этой системой

Самый простой и эффективный способ для out-of-band это использовать Burp Coloborator, мы можем использовать Burp Coloborator чтобы генерировать уникальные доменные имена, отправлять их вы качестве payloads приложению и отслеживать любые взаимодействия с этими доменами, если наблюдается входящий HTTP запрос, исходящий от приложение, значит оно уязвимо к SSRF

> [!warning] ВАЖНО!
> При тестировании SSRF часто наблюдается DNS-резолвинг переданного домена Coloborator, но без последующего HTTP запроса, обычно это происходит потому что приложение попыталось выполнить HTTP запрос к домену, что вызвало начальный DNS резолвинг, но сам HTTP запрос был заблокирован сетевой фильтрацией, довольно распространено что инфраструктура разрешает исходящий DNS трафик поскольку он необходим для многих целей но блокирует HTTP соединения к неожиданным пунктам назначения

## Пример Lab: Blind SSRF with out-of-band detection

В лабораторной прямо говорится что через заголовок Referer можно получить запрос к нашему Burp Coloborator серверу, поэтому используем это и меняем Referer на нужный нам адрес после чего лабораторная зачтется автоматически

---
Простое выявление уязвимости Blind SSRF способной инициировать out-of-band HTTP запросы, само по себе не обеспечивает путь к эксплуатации, так как вы не можете видеть ответ на бэкенд запрос, это поведение нельзя использовать для изучения содержимого на системах до которых может дотянуться сервер приложения, однако его можно задействовать для поиска других уязвимостей на самом сервере или на других бэкенд системах. Вы можете вслепую просканировать внутреннее IP адресное пространство, отправляя полезные нагрузки, предназначенные для детектирования широко известных уязвимостей, если эти нагрузки также используют слепые техники, вы можете обнаружить критическую уязвимость на непропатченном внутреннем сервере
## Пример Lab: Blind SSRF with Shellshock exploitation